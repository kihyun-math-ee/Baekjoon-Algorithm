# Two Pointers

This folder contains 12 BOJ solution files. Dates are the earliest recorded study dates in the original Study Log.

## 📅 Study Log

| Date | Category | Problem | Key Learnings |
| :--- | :--- | :--- | :--- |
| 2026-08-10 | Two Pointers | 1644 | Implemented the Sieve of Eratosthenes to efficiently generate an array of prime numbers up to N. Successfully applied a variable-size sliding window (Two Pointers) on the generated primes to count the number of continuous subarrays that sum exactly to the target value. Adequately handled edge cases for N <= 1. |
| 2026-08-12 | Two Pointers | 1253 | Implemented a two-pointer approach to identify numbers that can be formed by the sum of two other distinct numbers in a sorted array. Utilized a targeted $O(N^2)$ search, specifically handling the crucial edge case of excluding the target index itself (`left == i`, `right == i`) from the sum candidates to prevent self-referential false positives. |
| 2026-08-14 | Two Pointers | 1806 | Implemented a Two Pointers approach to dynamically adjust a sliding window, finding the minimum length of a contiguous subarray with a sum >= S. Optimized time complexity to $O(N)$ by sequentially moving `left` and `right` indices without redundant traversals, and successfully handled the edge case where the target sum is never reached by initializing the minimum length with infinity. |
| 2026-08-21 | Two Pointers | 2003 | Implemented a Two Pointers algorithm to efficiently find the number of contiguous subarrays that sum up to a specific target value. Optimized time complexity to $O(N)$ by dynamically updating the current sum through adding or subtracting values at the `left` and `right` pointers instead of recalculating, thereby preventing redundant array traversals. |
| 2026-08-22 | Two Pointers | 2230 | Implemented a Two Pointers algorithm after sorting the array to find the minimum difference between two elements that is at least $M$. Optimized execution time by triggering an early exit (`sys.exit(0)`) when the exact difference $M$ is found, avoiding redundant array traversals. Successfully handled state updates using a dynamic `min_diff` tracking variable initialized with `float('inf')` for safe boundary comparisons. |
| 2026-08-24 | Two Pointers | 2473 | Implemented a Two Pointers algorithm to find three elements whose sum is closest to zero. Optimized the search space by sorting the array and dynamically adjusting `left` and `right` pointers around a fixed pivot to evaluate the `current_sum`. Successfully managed state updates using absolute values and a `float('inf')` initialization for safe minimum distance tracking, completing the logic in $O(N^2)$ time complexity. |
| 2026-08-26 | Two Pointers | 2470 | Implemented Two Pointers algorithm on a sorted array to efficiently find the pair summing closest to zero with $O(N \log N)$ complexity, avoiding $O(N^2)$ brute force overhead. |
| 2026-08-29 | Two Pointers | 13144 | Optimized O(N^2) array traversal into O(N) using Two Pointers. Replaced heavy hash structures with a 1D boolean array (is_visited) to maintain optimal cache locality. Derived the number of valid sub-arrays mathematically via right - left in a single operation. |
| 2026-09-01 | Two Pointers | 2461 | Solved the representative selection problem by flattening a 2D class matrix into a 1D sorted array of tuples (value, class_index). Deployed a sliding window with two pointers and a frequency array to dynamically track represented classes, minimizing the max-min difference in O(NM log(NM)) time. |
| 2026-09-02 | Two Pointers | 2559 | Implemented an $O(N)$ sliding window algorithm to find the maximum contiguous sum in an array. Optimized memory and runtime by calculating the initial window sum and updating only the incoming and outgoing boundary elements, eliminating redundant calculations. |
| 2026-09-04 | Two Pointers | 20922 | Implemented a sliding window architecture utilizing two pointers to find the maximum contiguous subarray length. Constructed a hash map frequency counter using `dict(zip())` to track occurrences efficiently. Maintained O(N) time complexity by dynamically adjusting the `left` and `right` bounds whenever an element's frequency reached the threshold. |
| 2026-09-05 | Two Pointers | 22862 | Implemented an $O(N)$ sliding window algorithm using two pointers. Dynamically managed the maximum length of contiguous even numbers by tracking and limiting the count of odd numbers (deletions) within the current window. |
